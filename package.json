{
  "name": "fs-shadow-js",
  "version": "2.0.5",
  "description": "FS-Shadow Javascript",
  "main": "watcher.js",
  "type": "module",
  "scripts": {
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha --check-leaks --bail --full-trace --require esm",
    "coverage": "export NODE_ENV=test || SET \"NODE_ENV=test\" && export NODE_OPTIONS='--experimental-loader @istanbuljs/esm-loader-hook --no-warnings' || SET \"NODE_OPTIONS=--experimental-loader @istanbuljs/esm-loader-hook --no-warnings\" && nyc --reporter=text mocha --require esm --experimental-modules",
    "test:all": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha --check-leaks --bail --full-trace --require esm --experimental-modules '*.test.js'",
    "coverage:all": "export NODE_ENV=test || SET \"NODE_ENV=test\" && export NODE_OPTIONS='--experimental-loader @istanbuljs/esm-loader-hook --no-warnings' || SET \"NODE_OPTIONS=--experimental-loader @istanbuljs/esm-loader-hook --no-warnings\" && nyc  --reporter=lcov mocha --require esm --report lcovonly -- -R spec '*.test.js'",
    "coveralls": "export NODE_ENV=test || SET \"NODE_ENV=test\" && export NODE_OPTIONS='--experimental-loader @istanbuljs/esm-loader-hook --no-warnings' || SET \"NODE_OPTIONS=--experimental-loader @istanbuljs/esm-loader-hook --no-warnings\" && nyc  --reporter=lcov mocha --require esm --report lcovonly -- -R spec '*.test.js' && cat ./coverage/lcov.info | ./node_modules/coveralls-next/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akdilsiz/fs-shadow-js.git"
  },
  "keywords": [
    "fs",
    "virtual",
    "file-system"
  ],
  "author": "Abdulkadir Dilsiz <https://github.com/akdilsiz>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/akdilsiz/fs-shadow-js/issues"
  },
  "homepage": "https://github.com/akdilsiz/fs-shadow-js#readme",
  "devDependencies": {
    "@istanbuljs/esm-loader-hook": "^0.3.0",
    "coveralls-next": "^5.0.0",
    "esm": "^3.2.25",
    "js-sha256": "^0.11.1",
    "mocha": "^11.7.2",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^17.1.0",
    "unit.js": "^2.1.1"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.2",
    "uuid": "^11.1.0"
  }
}
